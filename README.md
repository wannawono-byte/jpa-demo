# jpa-demo
Spring Boot + Spring Data JPA + MySQL + Docker 기반 CRUD

# JPA 실무 데모

- Spring Boot 3 + Spring Data JPA + MySQL + Docker
- 1:N, N:1, N:N 관계 + Cascade + Fetch Lazy
- N+1 문제 해결 (fetch join + @EntityGraph)
- 실무 수준 엔티티 설계 및 연관관계 편의 메서드
